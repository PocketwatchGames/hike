// How the several InputMap actions of one controls-help column read together.
public enum EBindingJoin
{
	// Each works on its own — "Z / C".
	Alternatives,
	// Held together as one input — "LMB+RMB".
	Chord,
}
